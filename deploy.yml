---
  - name: Deploy Maven artifact locally
    hosts: localhost
    gather_facts: no

    tasks:
      - name: Copy JAR to target location
        copy:
          src: "target/mavenapp-1.0-SNAPSHOT.jar"
          dest: "/Users/nihal/code/labte/t.jar"